def anagrama(a, b):
    d_a = {}
    b_a = {}

    if len(a) == len(b):
        if d.get(num) != None:
            for chave in d:
                if chave == num:
                    d[chave] += 1
        else:
            d[num] = 1
    else:
        print("Não é um anagrama")